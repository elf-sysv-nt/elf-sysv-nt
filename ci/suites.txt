# Suite registry for ci/gate.sh. One executable per line, repo-relative.
# Each is run as: <suite> -n <count> [-q] and must exit non-zero on any
# failure, a fuzz crash included. Blank lines and # comments are skipped.
loader/elf/t/run.sh
# ci/t/run.sh is a meta-test of this gate: it installs the hook and drives real
# merges, so running it FROM the gate is reentrant and mutates the live repo
# (it left a "base" commit on march and flipped core.bare once). Run it
# standalone as CI-of-CI, never as a gate suite.
bin/check-doc-refs
bin/check-design-links
bin/check-worknote-refs
bin/check-carry-forward
# The veneer's own two. Neither was registered, so the DR-0079 withdrawal broke
# veneer/libc/t's surface assertion and the gate stayed green for two commits.
veneer/classification/t/reproduce.sh
veneer/xlat/t/reproduce.sh
# veneer/libc/t/run-tests.sh builds libc.so.6 and so needs the cross
# toolchain; test/suites.tsv calls it report for that reason. A gate suite
# that depends on one machine's build products blocks every merge elsewhere.
bin/check-suites
bin/check-shim-bodies
