# ci -- the merge gate (WP-T1, in progress)

Work in progress. This directory will hold the gate script that runs the
fixture corpus and fuzz target on the pinned verification root, the
pre-merge hook that blocks a merge when they fail, and the installer that
wires the hook into a checkout.
