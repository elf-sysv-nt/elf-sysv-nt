# reent-stub-faceload (WIP)

WP-56 reent-tls-bringup, item 1's face-base half. Measures whether a
real-process host of the faced runtime -- the DR-0060/0066/0067 shape:
`-nostdlib` against the WP-26 `crt0.o` and `-lcygwin` -- clears the
`error 1114` cygheap wedge that the plain-PE cygload host hits when it
`LoadLibraryA`s `elfsysv1.dll`.

Scaffold only; measure.sh and its transcript follow.
