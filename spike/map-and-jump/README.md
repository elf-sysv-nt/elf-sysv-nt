# Spike 2: map and jump

Unrun. Placeholder; the method belongs here once someone writes it.

**Question.** Can a PE stub map a static ELF image and transfer control to it,
with no dynamic linking in play?

**Gates.** The image mapping and initial process image layers. Poured
elsewhere, on other runtimes; this would be the first time on ours.

Per `AGENTS.md`, this directory holds the script that measures it, its sources,
and the transcript the script produced, named by the date of the run.
