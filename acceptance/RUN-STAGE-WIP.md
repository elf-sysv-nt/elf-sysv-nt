run stage (WIP): accept.sh launches a ready package through the loader crossing
instead of stopping at `ready`. Flips the run-stage-wired rung of
acceptance/to-green.tsv. Probe (2026-09-01): bzip2 halts at elf_map_err_granule
before entry -- recorded, so the harness names the obstacle rather than the wait.
