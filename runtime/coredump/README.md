# ELF core dumps

WP-61, in progress. A fatal signal leaves an ELF core the WP-60 gdb reads,
written by the runtime itself from the register record the delivery path
already captures. DR-0033 carries the decision; this directory carries the
writer and its tests.
