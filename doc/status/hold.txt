# WP ids set aside from autonomous building (still undelivered,
# just not built by the worker). Remove an id to release it.
# (empty: nothing held — the worker may build any dependency-ready package,
#  including WP-15, unattended.)
# reopened by DR-0038's audit: certified on 3.0.7, failed on 3.6.10.
# WP-22's hold lifted 2026-08-31 by the operator on the repaired, passing suite.
# WP-32's hold lifted 2026-08-31: the hint-discriminates redo certified on the primary root.
# WP-43's hold lifted 2026-08-31: the harness self-kill was root-caused and the
#   suite passes on the primary root; the reservation cost read -0.49% median,
#   inside DR-0006's under-5% band. Retiring -mno-red-zone stays a reserved call.
# WP-61's hold lifted 2026-08-31: the cross gdb was rebuilt on the primary root,
#   the SIGPIPE is gone, and runtime/coredump certifies 15 of 15.
# (nothing held)
# WP-56's hold lifted 2026-09-02 by the operator. The park was on a misreading:
#   the occupant of 0x400000 was the loader's own slurp buffer, not the faced
#   runtime, so none of the three candidates it was scored against is needed.
#   DR-0072 states the contract, a suite step asserts it, and the acceptance
#   harness now builds bzip2 with el8's own flags (spike/vendor-hardened-build).
# WP-T4 parked 2026-09-02: scope/authority, tier 8 — see the blocker log
WP-T4
