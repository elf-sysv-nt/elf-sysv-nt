# Not verified

Every claim under a Not verified heading in the governed set and in
the in-force decision records, with its source. This is the page the
convention in `AGENTS.md` has always implied and the tree has never
had: what the design currently rests on and nobody has measured.
Generated by `bin/refresh-status-reports.py`; not tracked.

89 unverified claim(s).

## From the governed documents

### doc/design/Requirements.md

- The number itself. It is the operator's to set and is a marked blank until they set it, so nothing in this document is yet a bar anything can be measured against.
- That 2893 is the right denominator. It is the el8 package count this project has used throughout, and no pass over the vendor manifests has confirmed it against the Rocky 8.10 source set DR-0002 pins.
- That the three classes partition the face. They were derived by reading what the records already assert, not by walking `veneer/libc/`'s tables and assigning every row; a symbol that fits none of the three is a finding about this document.
- That a package passing its own test suite means the platform is correct for that package. It means the package's authors' tests pass, which is a weaker claim, and the differential work in `doc/design/Verification-Plan.md` exists because of the gap between them.

### doc/design/Architecture.md

- That the summary in The ABI seam and the detail in `veneer:doc/design/ABI-Boundary.md` say the same thing, and likewise for the three other sub-documents. Nothing checks a section against the document it hands off to, and the lint over the classification tables that would check one of them is its own proposal.
- Three mechanisms in this document are built, certified and unwired, and that is the build order rather than a defect. `loader/fork/` is a closed island: nothing outside it includes its header or calls into it. `dl_open` and `dl_close` have no production caller anywhere. `elfcore_write` is reached only by its own test, and the default disposition a fatal signal produces is consumed only by one. Leaf to trunk means exactly this state — an element certified before anything depends on it — and the packages behind all three sit in `doc/status/delivered.txt`.
- What is owed is the attachment, and each has one place it attaches. The fork manifest is populated where the window's extent is known, which is the placement path; the audit is what proves it, since a region table that disagrees across the fork is what the child refuses on. The loader lock is taken by whatever first calls `dl_open` from a running process, not by `dl_open` itself, which is why the bracket is the driver's. The core writer is called from the fatal-signal path, which no record has written yet.
- One of the three is more than unwired. DR-0029's premise is that the low window *is* recorded in the manifest, stated in the present tense, and no code makes that integration. That is a destination reported as an arrival, the same pattern as the toolchain defaults above, and it is the reason a reader should not take the record's account of what crosses a fork as an account of what the tree does today.
- Signal delivery carries two further gaps its own record names. A signal whose target is executing inside `cygwin1.dll` is redirected wherever it is, with no equivalent of the vendor's deferral check, which is the largest untested case in that package. And the `SA_RESTART` decision function exists with no caller, so nothing consults it.
- Several counts and file lists in the records have drifted from the tree, and where this document states a number it states the tree's. The specific divergences are recorded in the sub-document that owns each subsystem.
- Nothing in the loader, the runtime or the veneer is registered in `ci/suites.txt`. The gate runs the ELF parser suite and three documentation checks. Every other certification named anywhere in the governed set is run by hand, which means the non-regression guarantee several records rest on is not enforced by anything.

### doc/design/ABI-Boundary.md

- That the five divergence kinds cover the face. They were derived from the fifty-one rows in `veneer/classification/semantic-review.tsv`, and that file says of itself that it is a first-pass seed expected to grow. A row whose divergence fits none of the five is the finding this section exists to invite.
- That the summary in `doc/design/Architecture.md` and this document agree. Nothing checks them against each other, and the lint over the classification tables that would check this document against `veneer/libc/` is not written.
- That `semantic-review.tsv`'s rows are correct. Its own header says every row is flagged for review rather than an asserted shim design, and a human confirming the divergence and the wrapper is a step that has not happened for most of them.
- That the fault path can be closed at all in the shape DR-0042 leaves open. Three candidate repairs are named and none is measured; until `fault.sh` reports `delivered` on a runtime-created thread, the upward direction of this seam is specified and not built.

### doc/design/Symbol-Resolution.md

- The version behaviour is certified against a constructed case, not against a second implementation. `spike/rtld-lift-survey/` names FreeBSD's `rtld-elf` as the oracle WP-44's binding decisions should be checked against, the way the lookup engine is checked against a real `ld.so`, and that harness was not built. The exec-path suite proves that one name in one library gives two answers to two differently-versioned references, which no first-definition scan can do; it does not prove that the answers agree with a second complete loader.
- The differential is not against el8's own 2.28. The plan asks for the WP-T2 environment and real glibc version nodes; the specimens `loader/exec/t/dyn-version.sh` builds carry `GLIBC_2.2.5` and `GLIBC_2.14` because those are the names the failure wears, not because a real `libc.so.6` was loaded. `doc/design/substitutions.md` is where that row belongs when the run happens.
- The `dl` layer still re-implements the matcher rather than plugging into it. `dl.c` carries its own `vsym_match` and its own node-name walk, and the copy is not equivalent to the original: it has no unversioned-base fallback, so an object without `.gnu.version` is skipped rather than accepted as a non-default; it compares a raw `vd_ndx` without masking off the hidden bit; and it hardcodes `0x7fff` and `0x8000` instead of naming the constants. Two matchers that disagree about the unversioned-base case will disagree about real libraries. The relocator's seam is wired and the `dl` surface's own lookup call is not, so a `dlopen`ed object is now relocated by one matcher and searched by another.
- The three-stage search order has no production user, and whether that is a defect is an open question rather than a known answer. Both non-test call sites — the `dl` surface's and now the relocator's — pass `NULL` for the local and the `dlopen` scopes, flattening everything into one scope built beforehand; the concatenation is exercised only by the unit test. Two readings survive and they lead different places. Either the flattened scope already produces the order the three stages would, in which case the three-stage signature is dead weight and the honest repair is to simplify the interface; or the flattening loses `RTLD_LOCAL` semantics, in which case it is a real defect. Deciding needs the two orders compared on a case carrying a local-scope-only definition, which the lookup suite can hold. That comparison is the next step, and adopting either reading before it is run would be a guess.
- The version matcher's check count is wrong, and one of its checks is a duplicate. The record and the README both say fifteen; the test contains thirteen assertions and prints a hardcoded 15 regardless. Two of the thirteen are the identical call, and the one labelled as testing predecessor implication does not reach that path, because the node it asks for is its own node in the fixture and matches before the verdaux chain is walked. The implication this document calls load-bearing is not independently asserted anywhere.
- DR-0013 misstates one library's ladder. It says `libcrypt.so.1` carries `GLIBC_2.2.5` through `GLIBC_2.4`; the extracted table gives it exactly three nodes, the base, `GLIBC_2.2.5` and `XCRYPT_2.0`, and all sixteen of its map rows use only the latter two.
- Two READMEs and one record carry counts the tree has moved past. `veneer:doc/design/what-the-veneer-lacks.md` reports 353 aliases and 192 shims against the current 323 and 222. `veneer/libc/README.md` still says every function in the library is a `ret`, which stopped being true when forwards became runtime-resolving thunks, and DR-0026 carries the same stale sentence; its symbol counts match neither the per-library nor the whole-map figures, and it says the build makes seven checks where it makes eight.
- The differential's floor is not el8's. The lookup differential compares against a WSL `ld.so` rather than the pinned 2.28, reads its verdict from `LD_DEBUG=bindings` rather than an exit status, and skips outright when no WSL `ld.so` is present.

### doc/design/Address-Space.md

- The reconciling fallback does not clear a real cygwin-linked child, and the two records that describe it were never amended. `veneer:spike/reent-stub-realproc-window-reconcile/results-2026-09-01.txt` reports `realproc_reserve_in=win_err_refused` against a low window that is `reserved+committed`, not the bare `MEM_RESERVE` DR-0068 models: two committed regions at `0x5fc000` and `0x5ff000`. `elf_window_plan` refuses any committed region, so reservation returns `win_err_refused` and adopt refuses likewise. DR-0071 sets the whole parent-handover arrangement aside for the sole-runtime shape and attributes the committed occupant to the foreign-parent arrangement rather than to a Cygwin runtime as such, but DR-0068 and DR-0069 carry no amendment and no pointer to it. Read them as live for the plain-PE shape only.
- `elf_window_release` was never taught what DR-0069 taught `elf_window_yield`. It still performs a single `VirtualFree(w->base, 0, MEM_RELEASE)`, which against a reconciled multi-reservation window frees only the allocation based at the window base and leaves the rest standing — precisely the bug DR-0069 fixed in `yield`. Its header forbids calling it on a `reserve_in` window and says nothing about an adopted one, which `elf_window_adopt` now accepts.
- The contract line and the enforced line are different numbers. Nothing in the tree enforces `0x100000000`. `realproc-file.c`'s upward scan starts at `ELF_WINDOW_BASE + ELF_WINDOW_SIZE`, which is `0x40000000`, and `loader/exec/t/low-window-stub.sh` asserts against the same figure. A host-side buffer at `0x50000000` satisfies every check in the tree and violates the rule this document states. DR-0072 is honest that its own consequence is a scan from the top of the window, so this is a tension inside the record rather than a code defect, but the enforced line is a quarter of the stated one.
- The address-space walk DR-0072 asks for is not built. What exists checks the image buffer's own address plus one symptom string, and it skips outright when the built `elfsysv1.dll` or `crt0.o` are absent, so on a fresh tree the invariant is not checked at all.
- A host-side bottom-up allocation survives in the plain-PE stub. The stub's `VirtualAlloc(NULL, opt.stack, ...)` for the guest stack names no base and does not scan. It runs after `elf_window_yield` and is normally safe because the remainders were re-reserved, but `yield` returns `win_ok` with `held == 0` when a remainder re-reservation fails, and the stub only prints about that and continues. In that path the window is bare and the next bottom-up allocation is the stack.
- The refusal DR-0064 asks for is not built. The program-facing `mprotect` is a plain forwarding thunk, and no wrapper names the granule or this rule; a sub-granule change returns a bare `EINVAL`. The loader's own refusal names the granule address and errno but not the granule size.
- The region walkers carry fixed capacities that neither record mentions. `reserve_in_around` and `elf_window_yield` both use 64-element arrays; a window fragmented past that returns `win_err_refused` or `win_err_release`, and the release path sets `held = 0` before it fails.
- `reserve.h`'s opening paragraph is stale in two ways. It tells DR-0028's single-release story, and DR-0072 claims it states the guest-ownership rule in that paragraph, which it does not. The per-function comments are current.
- `bzip2-nonpie` is red for a different reason than DR-0072 gives. It is described as failing at fixed-address loading; the 2026-09-02 acceptance run has it `ready` and `ran`, failing at the suite stage. The non-PIE image is the harness's own artifact rather than Red Hat's — the vendor build produces `e_type=DYN` at a zero load base — so the row exercises the harness, not el8.

### doc/design/Runtime-Crossing.md

- The cutover is not built. The acceptance harness still builds the plain-PE stub and passes only `--elf-runtime`, never `--runtime`, so `AT_BASE` is zero, every veneer thunk resolves against a null base, and the run exits 5 with no output. This is the single largest decided-but-not-built item behind this document.
- The record that decided the current position says what remains is the harness reshape "not a further measurement", and the tree disagrees. Driving real bzip2 against the actual faced arena on 2026-09-02, the bare hint is relocated to `0x6ffffffb0000` and a forced `MAP_FIXED` at `0x400000` is refused outright: `span of 0x50000 bytes at 0x400000 is occupied`. The clearing measurement was taken over a synthetic span; the real span fails. A further measurement over three named candidates is written up as the next step and has not been run, and is registered in neither `spike/` nor `test/spike-regen.tsv`.
- The record carrying the current position is `Proposed`, not accepted, while code has landed against it — the realproc window branches and the crossing host both exist.
- The dynamic driver does less than its record describes. That record names an object table, a dependency closure, a version-aware matcher and an `r_debug` announcement; the driver calls exactly three functions, adds two objects, and applies relocations. There is no object table, no closure walk and no debugger announcement in the exec path. It also runs three initializer tags where the record names two.
- The certification that record names has not been reached: bzip2 cross-built, run through the stub, its own `make test` executed and passed.
- The seam header and the seam have diverged. Five crossing functions are declared by hand at each use site rather than in the header, so a signature change will not be caught.
- "Crosses only for output" does not describe the layer named after it. Four of its crossings are not output: the three memory calls, which have to go through the runtime that replays them across `fork`, and the errno location. No record covers them, and the justification for them lives in a README.
- None of this area is gated. Neither the exec suite nor the realproc suite nor any of the face certifications is registered in `ci/suites.txt`, so the non-regression guarantee three of these records rely on is not enforced by anything. The crossing host's own driver makes no pass or fail assertion at all; it echoes a return code.
- Two file counts in the records are wrong: the realproc layer is described as three files and is five, and one record names a `realproc.c` that does not exist. The exec suite is described as a ten-check bar in one place, plans fourteen steps in another, runs a fifteenth that is not in its own plan, and is enumerated a fourth way in a third document.

### doc/design/Verification-Plan.md

- That class A comparisons actually read el8's artifacts everywhere they claim to. The vendored headers do; whether every constant table in the wiring layer was derived from them rather than typed from a manual has not been swept.
- That the gate's suite registry covers what the classes above require. The registry grows one line per package, which means its coverage is whatever the packages have contributed and not a set anybody has checked against this document.
- That the differential floor is reachable on demand. The seam resolves to a Rocky 8.10 WSL instance named `rocky8`, and nothing pins that instance's existence; a machine without it can run class B tests only against a substitute.
- That the three classes are provable at all for the fault path. The ABI boundary's upward direction is specified and not built, per DR-0042, and no bar in this document is currently satisfiable for it.

### doc/design/glibc-reuse.md

- That the coupling judgement has been made per file rather than per project. It has been made for glibc's resolver and for `dl-tls.c`, both named in records. No pass has walked glibc's source asking which routines would lift cleanly, and until one does, "most glibc source is coupled" is an impression formed from two examples.
- That the licence reading is legally correct. It is an engineering reading of licence texts and of the FSF's published guidance, made by people who are not lawyers and who have decided, for want of money, to proceed on that reading.
- That `%fs` is unavailable on every Windows this platform might target. The measurement is against one host. A Windows that preserved a user-written FS base would not make glibc usable on its own — the coupling and the loader assumptions would remain — but it would remove the reason stated here.

### doc/design/target-definition.md

- That el8 ships no non-PIE executables uniformly. Every object in the sample was `ET_DYN`, but the sample was three packages picked for other reasons; the PIE default above records that as el8's baseline, and WP-41 is where a non-PIE image, should the full set hold one, is handled by DR-0028's parent-side reservation.
- That `ELFSYSVNT` is unclaimed as a note owner. Nobody has grepped a distribution for it. The consequence of a collision is small and the check is cheap, so it is listed rather than done.
- The `release` string against real packages. The argument above is from how version parsers are written, not from having built anything against it. The first package that gates on a kernel version is where it gets tested, and if the suffix turns out to bother `rpm`'s own comparisons, the escape hatch is `4.18.0-1.elfsysvnt` rather than a return to a bare number.

## From the decision records

### DR-0027 — one classifier for the exec branch, and a four-hop interpreter limit

- The four-hop limit is matched to Linux from memory of `BINPRM_MAX_RECURSION` rather than from `git show` against a kernel at a known tag. The number is cheap to change and the shape is not, but a reader should treat the claim that it equals Linux's as unconfirmed until someone checks it against a source.
- Whether Cygwin's spawn path can be patched to call this classifier without disturbing the `#!` handling it already has is untested here: winsup is not in this tree, and the branch is certified through `elfsysv-exec`, a front end that calls it exactly as the spawn path will. The obligations that come with exec — descriptor inheritance and close-on-exec, the working directory, signal disposition, the environment — are discharged by that spawn path for the PE case already and are not re-implemented here, so nothing in this package has tested that they survive the ELF branch.

### DR-0028 — the low window is reserved by the parent, into a suspended stub

- The window is released and re-reserved on the loader's own thread before any other thread exists in the stub, which is why nothing is expected to allocate in the gap. That expectation is reasoned rather than measured: no test injects an allocation between the release and the placement, and none proves that the Cygwin DLL has no timer or worker thread of its own running by then.
- The measurement was taken on one host, on one Windows build, three times with the same result. The stack placement it turns on is a kernel policy with randomization in it, and a host that placed the initial stack elsewhere would change which cases pass without changing what the decision should be. The script is kept so the numbers can be retaken rather than believed.
- `0x3FC00000` is a judgment about how much room the ELF world wants low, not a measurement of how much it needs. Nothing yet allocates a brk or places a shared object near the executable, so the first package that does may find the figure too small or needlessly large.

### DR-0029 — what crosses the fork, and how the child knows

- The loader lock is modelled in the certification rather than held by `dl_open` itself. WP-38 delivered a single-threaded surface and deferred the per-thread error carrier to this package's thread work; the bracket is therefore applied at every entry into the loader by the driver, exactly as the surface will hold it, but the surface does not yet hold it itself. Moving the bracket inside `dl_open` and `dl_close` is work this package identifies and does not do.
- winsup is not in this tree, so the three phases are certified through `elfsysv-fork`, a front end that calls them as Cygwin's fork will. The claim that Cygwin's fork calls them at those three points is read from its source rather than measured against a modified `cygwin1.dll`.
- The rebase result is a measurement on one machine on one day, over a few dozen forks with one object loaded. It is evidence that the failure mode is not routine here; it is not evidence that it cannot happen under a different DLL population or under an address-space layout randomization setting this machine does not use.
- Descriptor inheritance, close-on-exec, signal disposition and the working directory are the spawn path's, not this package's, exactly as WP-41 left them.

### DR-0030 — the receiving thread builds the signal frame, and the return is an iretq

- That `iretq` remains available under a hardened Windows configuration. Control- flow enforcement, and shadow stacks in particular, interact with any return that is not a plain `ret`, and this host has neither enabled. The probe checks the machine it runs on and says nothing about a machine it has not run on.
- That the delivery is correct when it lands inside `cygwin1.dll` rather than in ELF code. Cygwin's own `interrupt_now` defers in that case, and this package does not yet: it redirects wherever the target happens to be. Deliveries in the certification landed in a hand-written leaf and in this package's own code, and a target inside the host runtime holding a host lock has not been tested.
- That the state is per thread. `elfsysv_sigstate_t` carries one blocked mask and one alternate stack, and POSIX gives each thread its own. The state travels through `elfsysv_sig_current`, which is per thread, but the certification uses one state for the whole process. The per-thread split belongs with the runtime's own `_cygtls` work.
- That `SA_RESTART` restarts a real interrupted host call. What is certified is `elf_sig_restart_after`, the decision a down-call wrapper consults, against every disposition. The wrapper that will consult it is DR-0009's and is not written yet.

### DR-0033 — a fatal signal leaves an ELF core, written by the runtime

- That gdb reconstructs the shared-library list from NT_FILE alone for a process under the stub; certified so far against synthetic images, since the loader cannot yet run a process to crash (the WP-15/WP-60 boundary). The fatal-path wiring itself is unwritten and carries this decision when it is.

### DR-0037 — the linking exception carries forward with the modified library, as accepted practice

- That Red Hat shares this reading. Practice is acquiescence, not confirmation; no written statement from the copyright holder addresses a fork's carriage of the exception, and none was sought.
- That MSYS2 or Git for Windows ever received private assurances. Their public trees show none, which is what was checked.

### DR-0074 — a lift is cleared by licence text and recorded practice; LGPL-2.1-or-later is open

- That any of this is legally correct. It is an engineering reading of licence texts and of the FSF's guidance, made by people who are not lawyers and who have decided, for want of money, to proceed on that reading. Every claim here is checkable against the cited texts; that is the most that is claimed.
- That libunistring's gnulib modules are in fact LGPLv2.1+ at the ref built from. The manual's licence appendix was read; the module headers were not, and gnulib's `modules/*` files carry the per-module licence field that would confirm it.
- That the compatibility matrix says what this record reports. The FAQ page was fetched and its index reached; the table body itself was not rendered by the tool used, and the reading is from memory of the table, which has been stable for years. Somebody should open it in a browser and check the "LGPLv2.1 or later" row against the "LGPLv3" column.
- That DR-0037's carry-forward reading holds. Everything about the runtime's linkability inherits that uncertainty, and this record adds none of its own.

### DR-0075 — the governing documents carry the citations, and a checker holds them

- That nine sections can home every record without a miscellany. The assignment in proposal 0006's execution brief was made by reading the records, and the skeleton pass is what tests it; a record that fits none of the nine earns a tenth section rather than a drawer.
- That the derived reverse map stays adequate. It answers "which section owns DR-0044" and cannot answer "which sections did DR-0044 change", which is a question nobody has needed yet and which the forward `Amends` field will answer for records filed from here on.
- That a state assertion catches what a commit-shaped one would. A session can still land the record and the prose in two commits on one branch, and the trunk never sees the gap. That is deliberate. What it cannot do is land the record and never write the prose.

### DR-0076 — the architecture document is sized by coherence, not by a word ceiling

- That the document is in fact implementable from directly. Nobody has tried to build a layer from it without opening a record, and until somebody does, the bar is a claim rather than a measurement. The honest test is a session that implements something from the prose alone and reports what it had to look up.
- That four sub-documents is the right number. Two more sections carry enough material to justify one — the thread pointer with its DTV shape, and the veneer's classification — and both were kept inline on the judgement that a reader of them is still reading the architecture. That judgement has not been tested by anyone reading it cold.

### DR-0077 — the window reconcile of DR-0068 and DR-0069 is live for the plain-PE shape only

- That the committed occupant is a property of the foreign-parent arrangement rather than of a Cygwin runtime. DR-0071 asserts the first reading and the measurement does not separate them: no transcript shows a cygwin-linked child created by an ordinary Cygwin parent, which is the control that would.
- That the plain-PE shape is one this project keeps. It is what the exec suite certifies today, and the acceptance crossing is scheduled to move to the sole-runtime shape. If that move completes and nothing else uses the parent handover, the scope this record draws is around a mechanism with no remaining consumer, and retiring it is a different decision from bounding it.

### DR-0078 — doc/ splits by what a file claims about the present: design, history, and the plans between them

- That `doc/history/` stays a shelf rather than becoming an attic. Nothing checks that a document there is still worth keeping, and the mechanism that would — a review date, a retirement rule — is not written. The three surveys earn their place by being cited; a handoff that nothing cites is a different case and this record does not settle it.

### DR-0079 — the veneer is a bootstrap and claims a derived surface; a name outside it is not exported

- That a package outside the acceptance set fails in the way this record claims. The link failure was reproduced by hand against `_IO_do_write`, and the weak reference was reproduced linking clean beside it, but no vendor package has been built against the reduced surface and refused.
- That the twenty bodiless retained members are the right answer. They satisfy verneed matching and they are the failure mode the change exists to remove, which is uncomfortable and is written here rather than smoothed over. The alternative — dropping the node and letting the load fail naming the version — was not measured against a real vendor binary, and choosing it would reverse a clause of an accepted proposal rather than interpret one.

### DR-0080 — relocation resolves through the lookup engine, and the versioning seam is wired rather than lifted

- The differential against `rtld-elf` was not run. The spike names FreeBSD's loader as the oracle WP-44's binding decisions should be checked against, the way WP-35 was checked against glibc's `ld.so`, and this package did not build that harness. The certification above is a constructed two-node case, not a comparison against a second complete implementation.
- The differential is not against el8's own 2.28. The plan asks for the WP-T2 environment and real glibc version nodes; the specimens here are cross-built and carry `GLIBC_2.2.5` and `GLIBC_2.14` because those are the names the failure wears, not because a real `libc.so.6` was loaded. `doc/design/substitutions.md` is where that row belongs when the run happens.
- The `dl` surface still carries its own `vsym_match` rather than the matcher. `loader/dl/dl.c` passes a matcher it builds itself into `elf_lookup`, and the divergences `veneer:doc/design/Symbol-Resolution.md` records for that copy stand unchanged. This record wires the relocator's seam and does not touch the `dl` one; `elf_reloc_apply`'s version refusal does now run for `dlopen`, because `dl.c` calls it.
- Nothing was measured about cost. The engine now probes a hash table per object instead of walking a symbol table, which should be faster, and builds a per-reference version ctx, which is new work; neither was timed, and no load-time number is claimed.

### DR-0081 — The auxv reports the CPU it runs on: `AT_HWCAP` is the `cpuid` leaf-1 `%edx` word, `AT_HWCAP2` is zero

- The reference kernel is not el8's. `AT_HWCAP2` was compared against the auxv the WSL2 kernel builds (6.18.33.2-microsoft-standard-WSL2) under a Rocky 8.10 userland, because that is the kernel this machine runs; el8's own 4.18 predates `HWCAP2_FSGSBASE` entirely and would report `0x0`, which this platform matches exactly. The substitution therefore makes the comparison stricter rather than weaker, and it is recorded as row S4 in `doc/design/substitutions.md` all the same.
- No consumer was observed reading either entry. The failure mode this repairs is argued from what `AT_HWCAP` is for, not from a program measured taking the slow path because the word was zero. Naming that program is what would turn the repair from reasoning into evidence.
- `AT_HWCAP` was compared on one CPU. The claim that a `cpuid` word must agree between two kernels on one machine is a claim about `cpuid`, not a measurement over a set of hosts, and this ran on one.

### DR-0082 — the el8-wide acceptance comparison is not a work package in this tree; the bar is one package

- That `rhelcyg-8.10` will carry the comparison. This record removes an obligation from this tree on the strength of another repository's scope, and nothing here checks that the other repository has taken it up. If it has not, the el8-wide comparison is currently nobody's, and that is a gap in the program rather than in this document.
