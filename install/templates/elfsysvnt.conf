# Seeded by elf-install; do not edit.  The directory lines under this
# header are the managed settings, reapplied over this pristine template
# on every run; anything else written here is gone by the next one.
