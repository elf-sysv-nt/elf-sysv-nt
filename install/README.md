# Installation (WP-63)

Work in progress. The installer and configurator for the platform: manifest
driven, idempotent per AGENTS.md. Derived configuration is reseeded from the
pristine templates here on every run; orphans from retired revisions are
removed rather than left; leftovers from an interrupted run are reaped before
anything else happens.
